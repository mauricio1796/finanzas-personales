/**
 * DEPRECATED: Este archivo es temporal para compatibilidad
 * USAR DESDE AQUÍ: import { useFinance, FinanceProvider } from '@/src/state'
 */

export { useFinance, FinanceProvider } from '../../state/FinanceContext';
export type { Transaction, User, Category } from '../../types';
